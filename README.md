# Сервис проверки доступности аудиторий (Service B)

Микросервис для проверки возможности бронирования аудиторий с учетом конфликтов, рабочего времени и типа бронирования.

## 🚀 Технологии

- Django 5.x
- Django REST Framework
- Python 3.10+
- SQLite (по умолчанию)

## 📋 Endpoints

### Веб-интерфейс
- `GET /` - Админ-панель с мониторингом запросов

### API
- `POST /api/check/` - Проверить доступность аудитории
- `GET /api/health/` - Проверка работы сервиса
- `GET /api/checks/` - Получить все проверки
- `GET /api/bookings-list/` - Получить все бронирования

## 📦 Формат запроса проверки
```json
POST /api/check/
Content-Type: application/json

{
  "room": "101",
  "date": "2026-02-15",
  "time_start": "10:00",
  "time_end": "12:00",
  "type": "lesson"
}
```

## ✅ Формат ответа - Доступна
```json
{
  "available": true,
  "message": "Аудитория доступна для бронирования"
}
```

## ❌ Формат ответа - Недоступна
```json
{
  "available": false,
  "reason": "Аудитория занята в это время"
}
```

## 🎯 Реализованные сценарии проверки

### Сценарий 1: Проверка конфликтов
Проверяет, не занята ли аудитория в указанное время.

**Пример:**
```json
// Запрос
{"room": "101", "date": "2026-02-15", "time_start": "10:00", "time_end": "12:00"}

// Ответ при конфликте
{"available": false, "reason": "Аудитория занята в это время. Конфликтов: 1"}
```

### Сценарий 2: Проверка рабочего времени
Проверяет, доступна ли аудитория в рабочие часы (08:00-20:00).

**Пример:**
```json
// Запрос на 22:00
{"room": "101", "date": "2026-02-15", "time_start": "22:00", "time_end": "23:00"}

// Ответ
{"available": false, "reason": "Аудитория работает только с 08:00 до 20:00"}
```

### Сценарий 3: Проверка типа бронирования
Проверяет корректность типа бронирования.

**Допустимые типы:**
- `lesson` - Урок
- `exam` - Экзамен
- `meeting` - Встреча

**Пример:**
```json
// Запрос с неверным типом
{"room": "101", "type": "party"}

// Ответ
{"available": false, "reason": "Неизвестный тип бронирования. Допустимые: lesson, exam, meeting"}
```

## 🔧 Установка и запуск локально
```bash
# Клонируйте репозиторий
git clone <your-repo-url>
cd availability_service

# Создайте виртуальное окружение
python -m venv venv
source venv/bin/activate  # Linux/Mac
venv\Scripts\activate     # Windows

# Установите зависимости
pip install -r requirements.txt

# Примените миграции
python manage.py migrate

# Запустите сервер
python manage.py runserver 8001
```

## 📊 Админ-панель

Откройте http://localhost:8001 для просмотра:
- Всех проверок доступности
- Статистики (одобрено/отклонено)
- Активных бронирований
- Автообновление каждые 5 секунд

## 🔗 Ссылки

- Локальная админ-панель: http://localhost:8001
- API Health Check: http://localhost:8001/api/health/

## 👥 Авторы

- Имя друга - Сервис проверки доступности (Service B)
- Ваше имя - Сервис бронирования (Service A)